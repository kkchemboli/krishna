name: CI Pipeline
on :
    push :
      branches:
        - main
    
jobs : 
  build:
    runs-on: ubuntu-latest

    steps:
      - uses : actions/checkout@v4

      - name : Display Message
        run: echo "Continous Integration Pipeline Executed Successfully"

      - name: List Files
        run : ls